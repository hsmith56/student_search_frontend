// Central feature flags for the frontend.
// Keep these as build-time constants when possible so unused code can be excluded.

export const ENABLE_DASHBOARD = false as const;
export const ENABLE_RPM = false as const;

